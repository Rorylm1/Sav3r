<div class="name-header">
<h2><%= @item.name %></h2>
</div>



<div class="wrapper">
  <div class="cards">
    <div class="each-card">
      <div class="image">
        <img src="<%= @item.image %>" alt="">
      </div>

      <div class="brand">
        <%=  @item.brand%>
      </div>

      <div class="volume">
        <%= @item.volume %>
      </div>

    <div class="each-card">
    <% @item.shops.each do |shop| %>
      <%= shop.name %>
      <p><strong>£<%= '%.2f' % shop.price %></strong></p>
      <br>
    <% end %>
    </div>
  <%= link_to "Add to Basket", basket_item_order_histories_path(@basket, @item), class: 'btn btn-success text-white category-button', style: 'padding-bottom: 25px; font-size: smaller; vertical-align: text-bottom; width: 80%; height: 15px; margin: 2px;  background-color: #06D6A0 !important; margin-left: 50px', data: {turbo_method: :post} %> <br>
  </div>
</div>
